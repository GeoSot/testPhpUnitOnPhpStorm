<?php



add_action('plugins_loaded', function () {
    // You could, for example, do something like this:

}, 1);


add_action('parse_request', function ($wp) {

    // You could, for example, do something like this:

    //$request = Request::createFromGlobals();

    //$routes = Routes::all();
    //$router = new Router($routes);

    //$router->handle($request, $wp);
});
